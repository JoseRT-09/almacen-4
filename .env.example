# ============================================
# CONFIGURACIÓN DE BASE DE DATOS
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=residence_management
DB_USER=postgres
DB_PASSWORD=postgres

# ============================================
# CONFIGURACIÓN DEL SERVIDOR
# ============================================
PORT=3000
NODE_ENV=development

# ============================================
# CONFIGURACIÓN DE JWT
# ============================================
JWT_SECRET=tu_secreto_super_seguro_cambialo_en_produccion
JWT_EXPIRES_IN=7d

# ============================================
# OPCIONES DE SINCRONIZACIÓN DE BASE DE DATOS
# ============================================
# IMPORTANTE: Configura estas variables según tu entorno

# DB_FORCE_SYNC=true
# ⚠️  PELIGRO: Recrea todas las tablas (BORRA TODOS LOS DATOS)
# Solo usar en desarrollo inicial o para resetear completamente la BD

# DB_ALTER_SYNC=true
# ✅ Recomendado: Actualiza las tablas sin perder datos
# Usa esto cuando actualices modelos (agregar campos, etc.)

# Si no se define ninguna, solo crea tablas que no existen (modo producción)

# ============================================
# CORS (para desarrollo con frontend separado)
# ============================================
# CORS_ORIGIN=http://localhost:4200
# Descomenta y ajusta si necesitas configurar CORS específicamente
